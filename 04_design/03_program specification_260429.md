# 1차 중간평가 통합 프로그램 스펙서 (Program Specification)
Date: 2026-04-29

## [Index 01] PM / LE (Member 1)

### 1. 자재 운송 소요 계획 조회 프로그램 (ZRB1SD0006)
* **구분**: 조회
* **Description**: 물류센터 관점에서 월별 출고 예정량 및 운송 요청량을 파악하여 본사에 자재 운송 요청을 전송하는 프로그램
* **핵심 기능**:
    * 변동 판매량에 대응하기 위해 DYNAMIC TABLE(`LVC_T_TABLE`) 구조 적용 및 필드 카탈로그 개수 동적 제어
    * CDS View 기반 월별 판매량·운송 요청량 취합 및 월별 재고량 공식 산출 대입
    * 시각화 고도화: 음수 데이터 적색 강조, 자재/저장창고별 구분선(청색) 및 가운데 정렬/볼드 처리
    * Hotspot 인터페이스: 출고 예정량 클릭 시 주문관리 프로그램으로 이동, 운송 필요량 클릭 시 운송 요청 프로세스 동적 수행
    * Dropdown List를 이용한 자재 간 즉시 이동(`SET CURSOR`) 기능 구현
* **관련 테이블**: 주문 관리 헤더/아이템 테이블, 자재 재고 테이블, 운송 요청 테이블, 월별 소요 취합 CDS View

### 2. 서비스 오더 생성 및 조회 프로그램 (SAPMZB1MM0005)
* **구분**: 트랜잭션
* **Description**: 서비스오더 생성 및 구매 정보 참조, 운송 경로와 현황을 시각화 관리하는 프로세스 통합 프로그램
* **핵심 기능**:
    * Dropdown List 조건 변경에 따른 구매/서비스 조회·생성 Subscreen 동적 분기 처리
    * `CL_GUI_PICTURE` 컨트롤러 활용, Custom Container 내 운송 경로 관련 이미지/아이콘 동적 생성 및 상세 지도 팝업 구현
    * 유효성 검증: 구매업체와 운송업체의 국가코드 불일치 시 에러 메시지 팝업 처리
    * Tabstrip 기반 구매 자재 정보 및 운송업체 레이아웃 이원화 조회
    * 데이터 연동: 운송 현황이 '원유 입고~플랜트 입고' 구간일 때 서비스 엔트리 생성 버튼 활성화 및 관련 프로그램 메모리 ID 이동 처리
* **관련 테이블**: 구매오더 헤더/아이템 테이블, 운송 현황 테이블, 국가별 운송 경로 마스터 테이블, Service Entry 항목 테이블

---

## [Index 02] CO (Member 2)

### 1. 공정 계획/실적 비교 분석 프로그램 (ZRB1CO0007)
* **구분**: 조회
* **Description**: 생산 공정별 계획 비용과 실제 발생 비용 데이터를 비교 분석하여 변동 시각화 및 의사결정을 지원하는 프로그램
* **핵심 기능**:
    * Screen 1000 기반 데이터 선조회 및 화면 Splitter 좌(소스 ALV 상하 배치)/우(비교 및 결과 ALV) 구성
    * 좌측 소스 행 더블클릭 시 우측 상단 현재 선택 슬롯 ALV로 데이터 이동 및 결과물ID(OUTMT) 일치 여부 LED 아이콘 제어
    * 비교 실행 시 17개 핵심 원가 항목에 대한 계획-실적 차이 금액 및 차이율(%) 자동 산출
    * 데이터 동적 연동: [새로고침] 실행 시 PP 모듈(`ZTB1PP0013`)에 추가된 신규 공정 데이터를 CO 마스터로 실시간 흡수 및 ALV 자동 반영 (PP 연계 예정)
    * 결과 엑셀 내보내기 기능 표준 구현
* **관련 테이블**: 공정 계획 비용 테이블 (`ZTB1CO0005`), 공정 실적 비용 테이블 (`ZTB1CO0006`), 생산 공정 마스터 연계 테이블 (`ZTB1PP0013`)

### 2. 원가 계산 및 등록 프로그램 (ZRB1CO0003)
* **구분**: 트랜잭션
* **Description**: 계획 및 실적 비용 소스를 기반으로 정유 공정별 단위원가를 자동 계산하고 일별 누적 등록 및 변동 추이 그래프를 구현한 프로그램
* **핵심 기능**:
    * Screen 100 입력 조건 세팅 및 전일 원가 기준일(오늘-1일) 자동 계산 필드 구현
    * 임시 원가 ALV 내 당일 단위원가(`DAYUC`) 인라인 편집 모드(`EDIT = 'X'`) 활성화 및 수정 시 최종 단위원가(`UNTAM`) 실시간 재계산 로직 구축
    * **2패스 원가 계산 로직**:
        * Pass 1 (1차 공정): `(원유비 + 1차공정비 + 촉매비) * 수율 / 100` 계산을 통한 반제품 생산원가(`SFAMT`) 및 당일 단위원가 산정
        * Pass 2 (2차 공정): 1차 반제품원가(`SFAMT`)를 후속 공정 소스로 흡수하여 `SFAMT + 2차공정비` 기준으로 최종 산출
    * 이동평균 개념 적용: 당일 원가와 전일 원가의 평균값으로 최종 단위원가(`UNTAM = (DAYUC + PREVU) / 2`) 생성
    * 이중 팝업 제어: 동일 일자/제품 데이터 존재 시 신규 버전 생성 여부 확인 팝업(Screen 200/300) 구현 및 원가 상세 테이블(`ZTB1CO0008`) 내 3개 라인 아이템(당일, 전일, 최종) 자동 분기 저장
    * 시각화: 수동 저장되지 않은 당일 데이터 존재 시 경고 팝업 가동, 우측 영역 날짜별 최종 단위원가 꺾은선 그래프(차트 컨트롤) 동적 구현
* **관련 테이블**: 공정 계획 비용 테이블 (`ZTB1CO0005`), 공정 실적 비용 테이블 (`ZTB1CO0006`), 반제품/완제품 원가 테이블 (`ZTB1CO0007`), 제품원가 결과 상세 테이블 (`ZTB1CO0008`)

---

## [Index 03] MM (Member 3)

### 1. 최적 원유 기반 구매오더 통합 관리 프로그램 (SAPMZB1MM0004)
* **구분**: 트랜잭션
* **Description**: 외부 피오리 시스템의 최적 원유 조건 데이터와 연동하여 구매오더의 CUD 및 수율 기반 의사결정을 통합 제어하는 프로그램
* **핵심 기능**:
    * 사용자 편의 기능: 최초 실행 시 가이드 팝업 자동 오픈 제어 및 '오늘 하루 이 창 열지 않기' 쿠키 로직 구현
    * 비즈니스 데이터 연동: BP 마스터 및 공급업체 정보 ALV 핫스팟 연동, 벤더 선택 시 구매 가용한 원유 데이터로 탭스트립 내 '공급업체별 자재 선택' ALV 스크린 동적 전환
    * 구매오더 아이템 유효성 체크: 최적화 자재 여부 검증 및 불일치 자재 입력 시 경고 메시지 출력과 단가·단위 자동 할당 차단 알고리즘 구현
    * 다차원 시각화 탭스트립:
        * TAB 1: 호출 시점 환율 연동 외화-원화 자동 환산 ALV 구성 및 유종별 연산품 수율 조회 드롭다운 차트 연동
        * TAB 2: 계절 가중치 반영 원유 수율 데이터 ALV 및 차트 동시 출력 처리
    * 트랜잭션 제어: 저장 전 '오류 확인' 유효성 검증 버튼 구현, 저장 시 총액 재확인 팝업 가동 및 문서유형 'SV' 전환 시 서비스오더 프로그램으로 메모리 ID 전달 후 자동 화면 전환(`SET PARAMETER / CALL TRANSACTION`)
    * 결재 프로그램 연계 인터페이스 및 헤더(단건)/아이템(ALV 가변 EDIT 모드) 수정·삭제 다건 트랜잭션 스크린 구축
* **관련 테이블**: 구매 최적화 조건 테이블 (`ZTB1MM0005`), 구매오더 헤더/아이템 테이블 (`ZTB1MM0006`, `ZTB1MM0007`), 공급업체/자재 마스터 관련 테이블

### 2. 정유 부피측정 통합 조회 및 오더 역추적 프로그램 (ZRB1MM0001)
* **구분**: 조회
* **Description**: 온도, 밀도 등 환경 변수에 따른 환산 부피 실측 데이터를 기반으로 구매·판매·생산 전사 오더의 흐름을 역추적하는 물류 제어 프로그램
* **핵심 기능**:
    * Selection-screen 내 오더 유형, 자재, 플랜트 등 전 필드 서치헬프 적용을 통한 입력 오류 원천 차단
    * Splitter 컨테이너 및 `CL_DD_DOCUMENT` 활용, 선택 조건 텍스트 래핑 출력 및 선택 오더 유형에 따른 동적 `LEFT OUTER JOIN` 인덱스 검색 구현
    * 성능 최적화: `SELECT *` 지양 및 필요한 필드 선별 추출, 필드 심볼(`FIELD-SYMBOLS`)을 활용한 대량 데이터 가공 속도 최적화 및 미부합 데이터 딜리트 플래그 전 처리 메모리 절감
    * 물류 단계 자동 판별: 측정번호 유무 및 수량 징후(양수/음수) 분석을 통해 입고(GR)/출고(GI) 단계를 시스템이 자동 판별하여 텍스트 출력
    * 가독성 개선: 측정 상태별 아이콘 및 셀 색상 제어(`LVC_S_SCOL`) 구현, 상단 컨테이너에 `add_link` 이벤트를 결합한 시각적 물류 상태 바(오더-부피측정-입고) 구축 및 링크 클릭 시 후속 문서 역추적 화면 호출
    * 계층형 뷰 구조화: `REUSE_ALV_HIERSEQ_LIST_DISPLAY`를 사용하여 오더 헤더와 자재 상세 아이템 간의 1:N 관계를 단일 스크린에 구현, 측정 완료 건 대상 자동 확장(`EXPAND`) 설정 및 대기 상태 품목 즉각 식별 구조화
* **관련 테이블**: 부피측정 테이블 (`ZTB1MM0020`), 구매오더 헤더/아이템, 판매오더 헤더/아이템, 생산계획 오더 헤더/아이템, 자재이동 문서 테이블 일체

---

## [Index 04] PP (Member 4)

### 1. 자재-공정 연결 관리 프로그램 (SAPMZB1PP0003)
* **구분**: 트랜잭션
* **Description**: 플랜트 및 공정별로 투입(Input)되는 원재료와 생산(Output)되는 연산품·반제품의 연결 관계를 정의하고 CUD를 관리하는 프로그램
* **핵심 기능**:
    * Screen 1000 조회 조건 구성 및 공정 ID 필드에 표준 딕셔너리 F4 Help(서치헬프) 기능 구현
    * 화면 분할 레이아웃 기반 좌/우측 자재 목록(투입/생산) 영역과 공정 선택 영역 배치
    * 데이터 자동 생성: 사용자가 마우스로 선택한 자재 및 공정 소스값을 바탕으로 연결 데이터 초안(`Draft`) 자동 완성 로직 구축
    * 비즈니스 제어 트랜잭션: 설정 완료된 자재-공정 마스터 매핑 테이블 내 신규 데이터 최종 저장 및 선택 행에 대한 실시간 DB 삭제 기능 구현
* **관련 테이블**: 생산 공정 마스터 연계 테이블 및 자재-공정 연결 CBO 테이블 일체

### 2. 정유 연산품 특화 자재 소요량 계획(MRP) 프로그램 (ZRB1PP0004)
* **구분**: 트랜잭션
* **Description**: 정유 산업의 연산품 동시 생산 특성을 반영하여 완제품 독립수요(PIR)로부터 원유 필요량을 역산하고 BOM을 기반으로 하위 소요량을 전개하는 프로그램
* **핵심 기능**:
    * Screen 1000 필수 실행 조건 제어(플랜트, MRP Area 필수 지정) 및 플랜트 가용 원유 선택용 Dropdown List 구현
    * **정유 연산품 특화 역산/전개 알고리즘**:
        * **1단계 (역산 프로세스 - 우측 상단 ALV)**: 특정 휘발유 수요 발생 시 LPG, 항공유, 나프타가 동시 산출되는 특성을 고려하여, 각 완제품의 수율 데이터를 기반으로 생산계획(PIR)에서 필요한 최소 원유 투입량을 하위 모듈에서 상위로 역산 전개(`FERT -> HALB -> ROH`)
        * **2단계 (순전개 프로세스 - 우측 하단 ALV)**: 확정된 최소 원유 투입량과 역산된 BOM을 근거로 하위에서 상위 자재로 재전개하여 추가 투입 원재료량 및 최종 생산 반제품/완제품 수량 자동 정합 산출(`ROH -> HALB -> FERT`)
    * 다건 트랜잭션 반영: 소요 일자, MRP 요소, 자재 번호, 자재 종류, 계산 소요량 및 정유 단위(BBL 등)를 산출하여 관련 마스터 및 세부 소요량 테이블 4개 구조(`ztb1pp0011`, `ztb1pp0012`, `ztb1pp0013`, `ztb1pp0014`)에 일괄 `INSERT/MODIFY` 처리
* **관련 테이블**: 독립수요계획 테이블, BOM 마스터 테이블, 생산 계획/오더 헤더 및 아이템 테이블 4종

---

## [Index 05] FI (Member 5)

### 1. 물류/판매 연동 전표 수동 생성 프로그램 (ZRB1FI0004)
* **구분**: 트랜잭션
* **Description**: 판매 오더, 출고 및 대금 청구 등 물류·판매 변동 문서번호를 참조하여 회계 전표를 수동 생성하고 데이터 무결성을 검증하는 프로그램
* **핵심 기능**:
    * 입력 예외 처리 가동: 참조문서번호 미입력 또는 오기입 후 조회 시 하단 상태바 경고 문구 출력 및 데이터 리셋으로 사용자 혼동 방지
    * 라디오 버튼 선택 조건에 따른 참조 소스 데이터 유효성 검증 알고리즘 구현 (불일치 시 화면 초기화)
    * 사용자 편의성 극대화: 유효한 참조번호 입력 시 우측 상단 ALV(`Reference Table`)에 헤더·아이템 결합 정보 동적 출력 및 전표 헤더 필수 데이터를 시스템이 자동 하드코딩 입력 후 비활성화(`Display Only`) 제어
    * 프로세스 흐름 제어: 헤더 적요 미입력 시 저장 차단 및 아이템 추가 단계 진입 원천 봉쇄, 수정 필요 시 '변경' 버튼 활성화 분기 처리
    * 복식부기 아이템 생성 및 시각화: Ref ALV 더블클릭 시 차변/대변 1세트 형태의 인라인 편집 아이템 자동 생성, 금액 컬럼 `DO_SUM` 활용 자동 차액 계산 및 팝업창을 통한 '최종전표예시' 시각적 사전 검토 단계 구현 후 DB 반영
* **관련 테이블**: 판매 오더 헤더 테이블(`ZTB1SD0006`), 출고 문서 헤더 테이블(`ZTB1SD0008`), 대금 청구 헤더/아이템 테이블, 자재이동문서 테이블, BP 조정 계정 마스터, G/L 마스터 테이블 등

### 2. 생산/원가 연동 전표 수동 생성 프로그램 (ZRB1FI0003)
* **구분**: 트랜잭션
* **Description**: 자재 이동 및 공정 총원가 내역 등 생산 과정에서 발생한 참조 전표를 기반으로 제조원가 회계 전표를 수동 생성하고 검증하는 프로그램
* **핵심 기능**:
    * 생산 참조문서번호 정합성 체크: 공정 변동 문서번호 유효성 검증 및 예외 발생 시 하단 메세지 팝업 처리
    * 화면 동적 제어: 참조 테이블 ALV 데이터 바인딩과 동시에 전표 생성에 필수적인 필드 자동 입력 및 수정 제한 락(Lock) 처리 구현
    * 순차적 트랜잭션 가이드: 헤더 적요 검증 완료 후 단계별 비활성화 전환, 중간 수정 단계('변경' 버튼) 분기 구축을 통한 전표 데이터 왜곡 방지
    * 차대변 매핑 로직: 생산 참조 ALV 더블클릭 이벤트를 캐치하여 차변/대변 계정 과목 및 금액 1세트를 예시 리스트에 동적 배치, `DO_SUM` 집계 속성 적용
    * 이중 인터페이스 팝업: 가작성 완료된 헤더/아이템 회계 데이터를 전송 전 모달 팝업 스크린으로 최종 시각화하여 데이터 최종 검증 유도 후 전표 마스터 테이블 적재
* **관련 테이블**: 자재이동문서 헤더/아이템 테이블(`ZTB1MM0011`, `ZTB1MM0012`), 총원가 내역 테이블(`ZTB1CO0007`), G/L 마스터 테이블, 계정 결정 매핑 테이블(`ZTB1MM0017`), 환율 테이블

---

## [Index 06] FI (Member 6)

### 1. 전사 통합 회계 전표 대시보드 프로그램 (ZRB1FI0001)
* **구분**: 조회
* **Description**: 구매, 판매, 생산 모듈에서 생성된 전표 및 역분개 전표 전체를 통제하고 모니터링하는 총괄 회계 대시보드
* **핵심 기능**:
    * Screen 1000 제어: 회사코드 필수 서치헬프 적용 및 회계연도 필드 내 미래 연도 기입 원천 차단 로직 구현
    * 마스터-디테일 ALV 구현: 헤더 데이터 더블클릭 이벤트를 캐치하여 하단에 종속된 전표 아이템 내역 실시간 바인딩
    * 팝업 인터페이스: 전표 헤더 ALV 내 참조문서번호 핫스팟 클릭 시, 원천 물류·마스터 문서 세부 정보를 동적 모달 팝업으로 출력
    * 시각적 가시성 개선: 차변/대변 행의 금액 필드 위치를 시각적으로 분리 배치하고, 대변(Cr) 컬럼에 특정 색상 코드 바인딩 및 총계 라인 `DO_SUM` 적용을 통한 대차 차액 점검 최적화
    * 조건별 다차원 필터링: 전체/구매/판매/생산 모듈별 전표 필터 라디오 버튼 및 역분개 전표 전용 조회 체크박스 서브스크린 동적 연동 구성
    * 트랜잭션 네비게이션: 타 모듈 전표 수동 생성 프로그램으로의 즉시 화면 전환 버튼 배치 및 전표 생성 후 복귀 시 자동 리프레시 반영
* **관련 테이블**: 전표 헤더 테이블(`ZTB1FI0001`), 전표 아이템 테이블(`ZTB1FI0002`)

### 2. 구매/물류 참조 전표 반자동 생성 프로그램 (ZRB1FI0002)
* **구분**: 트랜잭션
* **Description**: 원유 구매 물류 선적입고, 송장, 플랜트 입고 시점의 원천 전표를 참조하여 복식부기 회계 전표를 반자동 생성 및 검증하는 프로그램
* **핵심 기능**:
    * 시점별 라디오 버튼 및 물류 유형에 맞는 동적 서치헬프 제안 로직을 통해 사용자 오기입 최소화
    * 조회 마스터 테이블 바인딩 시 자재이동문서 및 송장문서 헤더/아이템을 동적 조인하여 UI 유도 후 헤더 데이터 자동 매핑 및 비활성화 처리
    * 순차적 락(Lock) 제어: 헤더 적요 검증 완료 전까지 참조 테이블 더블클릭을 차단하며, 저장 완료 시 헤더 영역 비활성화 및 버튼 텍스트를 '변경' 모드로 동적 변환
    * 아이템 전표 동적 1세트(차대변 2줄) 자동 생성 및 수동 적요 텍스트 동기화 유지 알고리즘 구현
    * 중복 저장 방지: 회계 전표가 이미 생성된 마스터 정보 재접근 시 트랜잭션을 차단하는 예외 처리 엔진 가동
    * 회계 합산 최적화: 최종 전표 예시 팝업 호출 시, 금액 필드를 제외한 모든 속성이 일치하는 동일 차변/대변 데이터를 시스템이 자동 합산하여 단일 행으로 압축 정렬 출력 구현
* **관련 테이블**: 전표 헤더/아이템 테이블, 자재이동문서 헤더/아이템, 송장문서 헤더/아이템, 계정 결정 매핑 테이블(`ZTB1MM0017`), GL 마스터 등

---

## [Index 07] SD (Member 7)

### 1. 물류센터 기준 판매 주문 통합 관리 프로그램 (ZRB1SD0001)
* **구분**: 조회
* **Description**: 비즈니스 파트너(BP) 기준으로 판매 오더를 트래킹하고 거점 물류센터 단위의 출고 현황을 종합 관리하는 프로그램
* **핵심 기능**:
    * Screen 1000 거점 선택 파라미터 제어를 통해 초기 진입 시 지정된 물류센터 소속 판매 오더만 필터링 바인딩 처리
    * 트리-ALV 복합 아키텍처: 좌측 BP 트리 노드 클릭 시 상세 BP 마스터 팝업 가동과 동시에 우측 영역 판매오더 헤더 ALV 연동 호출 구현
    * 마스터-디테일 전개: 헤더 ALV 더블클릭 시 품목 세부 아이템 ALV 종속 출력
    * **오더 상태 대시보드 박스 구현**: 
        * 주문 오더를 진행 상태(REQ, RDY, LOD, BIL, CLO)별로 실시간 집계하여 상단 상태 박스에 수치화 표시
        * 특정 상태 박스 클릭 시 해당 조건에 부합하는 데이터만 헤더 ALV에 동적 필터링 바인딩 처리
    * 프로세스 흐름 연결: '출하 준비(RDY)' 오더 선택 후 툴바의 [출고하기] 실행 시 후속 출고 관리 트랜잭션 프로그램으로 파라미터 자동 전달 및 화면 호출
* **관련 테이블**: BP 일반 마스터(`ZTB1SD0001`), 판매오더 헤더/아이템 테이블(`ZTB1SD0006`, `ZTB1SD0007`), 출고 헤더/아이템 테이블, 청구 데이터 취합 CDS View

### 2. 판매 기반 출고 처리 및 이력 관리 프로그램 (SAPMZB1SD0001)
* **구분**: 트랜잭션
* **Description**: 판매 주문 참조 기반 실시간 출고 트랜잭션 처리와 거점별 출고 이력 추적 기능을 단일 아키텍처에 구현한 프로그램
* **핵심 기능**:
    * **Screen 100 (출고 처리 트랜잭션)**:
        * 주문관리 프로그램에서 전달받은 판매오더 및 물류센터 파라미터를 수신하여 화면 진입 시 헤더 및 거래 정보 자동 세팅 구현
        * 실시간 재고 가시성: 선택 물류센터의 제품별 현재 가용 재고 수량을 실시간 바인딩하여 수치 가이드 제공
        * 발주 수량 동기화: [납품 수량 채우기] 버튼 클릭 시 주문 잔량 기반 납품 수량 일괄 자동 맵핑 기능 구현
        * **출고 확정 단일 트랜잭션 셋**: [출고하기] 실행 시 출고 테이블 데이터 저장, 당일 출고일 세팅, 오더 상태 'LOD' 업데이트, 물류센터 가용 재고 즉시 차감 및 작업 하단 로그 세부 기록 일괄 제어
        * 시각화: 재고확인 박스 내 출고 수량만큼 마이너스(`-`) 변동 필드 동적 바인딩
    * **Screen 200 (출고 이력 모니터링)**:
        * 거점 물류센터 기준 출고 완료 데이터를 추적하되, [전체 보기] 체크박스 선택 시 센터 조건을 무시하고 전체 오더 데이터 인덱스 스캔 전환
        * 실 출고 처리가 완료된 건만 ALV 툴바 내 체크박스 아이콘으로 가시성 극대화 및 마스터-디테일 더블클릭 뷰 구현
* **관련 테이블**: 출고 헤더/아이템 테이블(`ZTB1SD0008`, `ZTB1SD0009`), 자재이동 문서 헤더/아이템 테이블, 자재별 재고/플랜트 정보 테이블(`ZTB1MM0003`), 판매오더 헤더 테이블
